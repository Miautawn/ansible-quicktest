---
- name: "{{ ansible_facts['distribution'] }} | FLATPAK SETUP | Looking for distro-specific task..."
  ansible.builtin.include_tasks: "{{ item }}"
  with_first_found:
    - files:
        - "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}.yaml"
      skip: true