---
- name: "{{ ansible_facts['distribution'] }} | GPU DRIVERS | Looking for distro & vendor specific task..."
  ansible.builtin.include_tasks: "{{ item }}"
  with_first_found:
    - files:
        - "{{ role_path }}/tasks/{{ ansible_facts['distribution'] }}_{{ gpu_vendor }}.yaml"
      skip: true